apply plugin: 'com.android.application'

android {
    compileSdkVersion 7
    buildToolsVersion "24.0.1"

    defaultConfig {
        applicationId "es.pymasde.blueterm"
        minSdkVersion 7
        targetSdkVersion 7
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
